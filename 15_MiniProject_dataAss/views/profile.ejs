<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Profile</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="h-screen w-full bg-zinc-900 flex items-center justify-center p-6">
        <div class="bg-zinc-800 p-8 rounded-xl shadow-lg w-full max-w-md">
            <h1 class="text-2xl font-bold text-white mb-6 text-center">Profile Details</h1>
            <div class="space-y-4">
                <p class="text-white"><span class="font-semibold text-indigo-400">Name:</span> <%= user.name %></p>
                <p class="text-white"><span class="font-semibold text-indigo-400">Age:</span> <%= user.age %></p>
                <p class="text-white"><span class="font-semibold text-indigo-400">Email:</span> <%= user.email %></p>
                <p class="text-white"><span class="font-semibold text-indigo-400">User ID:</span> <%= user.userid %></p>
            </div>
            <div class="mt-6 flex flex-col gap-3">
                <a href="/logout" class="block w-full text-center p-3 rounded-lg bg-indigo-600 text-white font-semibold hover:bg-indigo-700 transition duration-300">
                    logout
                </a>
                <a href="/" class="block w-full text-center p-3 rounded-lg bg-indigo-600 text-white font-semibold hover:bg-indigo-700 transition duration-300">
                    Go Home
                </a>
            </div>
        </div>
    </div>
</body>
</html>
